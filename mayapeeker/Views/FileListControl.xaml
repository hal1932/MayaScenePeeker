<UserControl x:Class="mayapeeker.Views.PreviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions" 
             xmlns:conv="clr-namespace:mayapeeker.Views.Converters"
             mc:Ignorable="d">
    
    <UserControl.Resources>
        <conv:PathNameConverter x:Key="name"/>
    </UserControl.Resources>
    
    <Grid Name="_container">
        <ListBox ItemsSource="{Binding DirectoryContainer.ItemList}"
                 SelectedIndex="{Binding SelectedDirectoryIndex, Mode=OneWayToSource}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ContentControl Content="{Binding FileSystemInfo, Converter={StaticResource name}}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseDoubleClick">
                                <ei:CallMethodAction
                                    TargetObject="{Binding DataContext, ElementName=_container}"
                                    MethodName="SelectItem"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </ContentControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
